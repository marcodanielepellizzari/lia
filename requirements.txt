Django>=5.0,<6.0
djangorestframework>=3.15
django-guardian>=2.4          # per-object permissions (foundation for future sharing features)
django-filter>=24.2           # filters for the analysis page (DRF)
scipy>=1.13                   # gaussian_kde for the deposit density overlay on the Pb-Pb diagrams
numpy>=1.26                   # grid building for the kernel density estimate
pandas>=2.2
openpyxl>=3.1          # reads/writes .xlsx/.xlsm
xlrd>=2.0.1             # reads legacy .xls (openpyxl does not handle .xls)
psycopg2-binary>=2.9           # driver Postgres (consigliato in produzione; sqlite ok in dev)
python-dotenv>=1.0
